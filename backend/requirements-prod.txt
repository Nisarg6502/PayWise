# Production requirements for the Cloud Run image.
# Same as requirements.txt but without `unstructured[md,pdf]` (heavy native deps for
# PDF/DOCX parsing) — matches current local dev behavior where non-.md/.txt rule
# uploads return a 400 telling the user to paste text or use .md/.txt instead.

# --- Web framework ---
fastapi>=0.115
uvicorn[standard]>=0.30
sse-starlette>=2.1          # Server-Sent Events for streaming LLM responses
python-multipart>=0.0.9

# --- Settings / validation ---
pydantic>=2.8
pydantic-settings>=2.4

# --- Relational database (PostgreSQL) ---
sqlalchemy>=2.0
sqlmodel>=0.0.21
psycopg2-binary>=2.9
alembic>=1.13

# --- Vector database ---
qdrant-client>=1.10

# --- Auth (Google OAuth 2.0 + JWT) ---
authlib>=1.3
python-jose[cryptography]>=3.3
httpx>=0.27
itsdangerous>=2.2           # session middleware for OAuth state

# --- Agent orchestration ---
langgraph>=0.2
langchain-core>=0.3
langchain-ollama>=0.2       # Qwen3 (LLM) + nomic-embed-text (embeddings) via Ollama
ollama>=0.3

# --- Observability ---
langfuse>=2.50,<3.0          # LangGraph/LangChain CallbackHandler tracing

# --- Embeddings + reranker ---
# Default providers are the Jina AI APIs (JINA_API_KEY) — nothing to install.

# --- Document parsing / ingestion ---
langchain-text-splitters>=0.3
